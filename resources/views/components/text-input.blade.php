<input type="text" placeholder="{{ $placeholder }}" name="{{ $name }}" value="{{ $value }}"
    {{ $attributes->class(['outline-none border-none font-open-sans text-xs w-full p-0 focus:ring-0 bg-transparent']) }} />
